apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: {{ .Values.api.service.type }}
  selector:
    app: api
  ports:
    - protocol: TCP
      port: {{ .Values.api.service.port }}
      targetPort: 8000
