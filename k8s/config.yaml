apiVersion: v1
kind: ConfigMap
metadata:
  name: sentinel-config
  namespace: sentinel
data:
  # API Configuration
  API_HOST: "0.0.0.0"
  API_PORT: "8000"

  # Model Configuration
  MODEL_PATH: "models/fraud_xgboost.onnx"

  # Infrastructure Endpoints
  REDIS_HOST: "redis-service"
  REDPANDA_BROKERS: "redpanda-service:9092"
  REDPANDA_BROKER: "redpanda-service:9092"